[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    e2e: End-to-end tests requiring live credentials and services
    slow: Tests that take more than a few seconds to run
asyncio_mode = auto
